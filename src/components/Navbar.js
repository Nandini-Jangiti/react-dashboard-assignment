import React from 'react';
import '../cssfiles/Navbar.css'

const Navbar = () => {
    return (
        <nav className="navbar">
            <div className="navbar-logo">Home Page</div>
        </nav>
    );
};

export default Navbar;
